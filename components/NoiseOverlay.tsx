"use client";

/**
 * Static SVG noise layer applied through `.grain` in globals.css.
 * Kept as a no-op component so the layout tree mirrors the
 * structure of the other chrome elements and is easy to swap later.
 */
export function NoiseOverlay() {
  return null;
}
